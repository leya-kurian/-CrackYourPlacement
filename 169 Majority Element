class Solution(object):
    def majorityElement(self, nums):
        d={}
        for i in nums:
            if i in d:
                d[i]+=1
            else:
                d[i]=1
        max=0
        index=-1
        for i in d.keys():
            if max<d[i]:
                max=d[i]
                index=i
        return index


        
        
