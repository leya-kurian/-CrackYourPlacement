class Solution(object):
    def removeDuplicates(self, nums):
        k=0
        for i in nums:
            c=nums.count(i)
            if c!=1:
                for j in range(c-1):
                    nums.remove(i)
                    k+=1
        

        
